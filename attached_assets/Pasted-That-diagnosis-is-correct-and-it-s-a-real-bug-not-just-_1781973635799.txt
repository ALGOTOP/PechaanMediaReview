That diagnosis is correct and it's a real bug, not just a guess — it lines up exactly with your GSC numbers. Hash anchors (`#work`, `#services`, `#contact`) aren't separate URLs to Google, they're fragments of the same page. So when your sitemap lists them as if they were distinct pages, Google discovers 4 "URLs," canonicalizes 3 of them back to the homepage as duplicates, and only the homepage actually gets indexed. That's your "1 indexed, 3 not indexed" exactly.

Go ahead and approve the three fixes — they're correct and low-risk:
1. Strip the anchor URLs from the sitemap, keep only real pages (homepage + AuditReport)
2. Make sure AuditReport is actually in there
3. Fix heading hierarchy (this helps Google and AI search engines understand page structure — relevant given your Semrush AI Visibility was also 0)

**But — important caveat, don't let this become "we fixed SEO."** Cleaning the sitemap fixes the indexing bug. It does not fix the 0 organic traffic / 0 organic keywords problem. Those are two different diseases:

- **Indexing bug** = Google can't even see your pages properly → fixed by what Replit's proposing
- **Zero rankings** = even once indexed, a one-page site with sections like "Brand Identity," "Film & Production," "Web & Digital" all crammed under one URL can only ever rank for *one* keyword cluster, because Google has nothing to rank for "brand identity agency Pakistan" *and* "film production agency Pakistan" separately — it's all the same page. Your competitors who rank have a dedicated URL per service.

So after Replit ships this fix, two more things matter more than this one:
1. **Resubmit the sitemap in GSC** and use URL Inspection → Request Indexing on the AuditReport page specifically (don't just wait for Google to recrawl on its own schedule)
2. **Decide if you actually want to invest in real SEO** — that means turning 2-3 of your service sections into real indexable subpages with unique content (`/services/brand-identity`, `/services/film-production`, etc.) at some point. Not urgent today, but worth knowing the sitemap fix alone won't move your 0% organic traffic.

Tell Replit to ship the three fixes. Once it's live, come back and I'll walk you through resubmitting in GSC and requesting reindexing properly.